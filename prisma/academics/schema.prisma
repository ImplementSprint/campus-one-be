generator client {
  provider = "prisma-client"
  output   = "../../src/generated/prisma/academics"
}

datasource db {
  provider = "postgresql"
}

// Academics domain models will be added during the service migration.
