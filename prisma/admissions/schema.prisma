generator client {
  provider = "prisma-client"
  output   = "../../src/generated/prisma/admissions"
}

datasource db {
  provider = "postgresql"
}

// Admissions domain models will be added during the service migration.
