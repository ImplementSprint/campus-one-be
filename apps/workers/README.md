# Workers

Optional NestJS worker apps belong here when a background job, queue consumer, or scheduled process needs an independent runtime from `apps/gateway`.

Do not add deployable worker apps until there is a real runtime isolation or scaling need.
